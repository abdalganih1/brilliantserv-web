# Template 3 - مؤسسة روائع الكريستال

قالب موقع احترافي للأعمال الميكانيكية والكهربائية وخدمات لف المحركات.

---

## 📁 بنية المشروع

```
template-3/
├── index.html              # الرئيسية
├── about.html              # من نحن
├── services.html           # الخدمات
├── projects.html           # المشاريع
├── contact.html            # اتصل بنا
├── blog.php                # المدونة (Dynamic)
│
├── blog/                   # مجلد المقالات (يحتوي على ملفات .php للمقالات)
├── components/             # ومكونات الواجهة (Nav, Footer)
├── css/                    # ملفات التصميم
├── js/                     # ملفات الجافا سكربت
├── includes/               # ملفات PHP المشتركة (Header, Footer, Config)
├── media/                  # الصور والملفات
│   ├── projects.json       # قاعدة بيانات المشاريع
│   └── services/           # صور وتفاصيل الخدمات
└── deploy.sh               # سكريبت النشر للسيرفر (Server-side script)
```

---

## 🤖 بروتوكول نشر وكلاء الذكاء الاصطناعي (AI Agent Deployment Protocol)

**هام جداً لأي نموذج ذكاء اصطناعي يعمل على هذا المشروع:**

لضمان سلامة العمليات واستمراريتها، اتبع الخطوات التالية بدقة عند إجراء أي تعديلات:

1.  **المصدر الوحيد للحقيقة**: هذا الملف (`README.md`) وملف `../ssh_connection/SSH_GUIDE.md` هما المصدران الأساسيان لفهم بنية المشروع وآلية النشر.
    *   🛑 **تنبيه هام**: قبل تنفيذ أي أمر في التيرمينال، **اقرأ ملف [`terminal.md`](../../terminal.md) الموجود في الجذر فوراً**. هذا الملف يحتوي على قائمة الأخطاء الشائعة (مثل مشاكل Git Bash) وسيوفر عليك محاولات فاشلة وتوكنز ضائع.
2.  **إدارة النسخ (Version Control)**:
    *   أي تعديل في الكود **يجب** رفعه إلى GitHub أولاً.
    *   لا تحاول النشر المباشر عبر SSH دون عمل Push، لأن نظام النشر الآلي يعتمد على سحب التغييرات من Git.
3.  **النشر (Deployment)**:
    *   بعد التأكد من `git push`، اقرأ ملف الدليل التالي بعناية فائقة لتنفيذ عملية النشر:
    *   --> **[دليل الاتصال والنشر (SSH_GUIDE.md)](../../ssh_connection/SSH_GUIDE.md)** <--
    *   هذا الملف يحتوي على الأوامر الدقيقة للاتصال بالسيرفر وتشغيل سكريبت التحديث.
4.  **التوثيق الذاتي (Self-Documentation)**:
    *   إذا واجهت مشكلة وحللتها، أو أضفت ميزة جديدة، **يجب** تسجيلها في سجل التغييرات الموجود في `SSH_GUIDE.md` فوراً. هذا ليس اختيارياً.

---

## 📊 نظام البيانات (Hybrid JSON Loading)

### كيف يعمل؟

الموقع يستخدم **نظام تحميل هجين** للبيانات:

1. **مع سيرفر (استضافة)**: يحمّل البيانات من ملفات JSON المنفصلة
2. **محلياً (file:///)**: يستخدم البيانات المضمنة في HTML كـ fallback

```javascript
// Hybrid: Try fetch, fallback to embedded
try {
    const response = await fetch('media/projects.json');
    data = await response.json();
    console.log('✓ Loaded from JSON');
} catch (e) {
    data = embeddedData;
    console.log('✓ Using embedded data (file:// mode)');
}
```

### ملفات JSON

| الملف | المسار | الوصف |
|-------|--------|-------|
| `projects.json` | `media/projects.json` | جميع المشاريع مصنفة |
| `services.json` | `media/services/services.json` | جميع الخدمات |

---

## 🖼️ تصنيفات الصور

### 1. الأعمال الكهربائية (`media/electrical/`)
**26 مشروع** - أعمال التمديدات واللوحات الكهربائية

| النوع | الكمية | مثال |
|-------|--------|------|
| صور webp أصلية | 8 | `اعمال-كهرباء-1.webp` |
| صور PDF المستخرجة | 18 | `p17_img4.jpeg` |

### 2. الأعمال الميكانيكية (`media/mechanical/`)
**20 مشروع** - مضخات، أنظمة تكييف، أنابيب

| النوع | الكمية | مثال |
|-------|--------|------|
| صور webp أصلية | 8 | `اعمال-ميكانيكا-1.webp` |
| صور PDF المستخرجة | 12 | `p10_img4.jpeg` |

### 3. لف المحركات (`media/winding/`)
**2 مشروع** - إعادة لف وصيانة المحركات

| الملف | الوصف |
|-------|-------|
| `لف-مواتير-1.webp` | إعادة لف محرك 50 حصان |
| `لف-مواتير-2.webp` | صيانة عازلية المحركات |

### 4. الخدمات (`media/services/`)
**11 خدمة** - الخدمات المقدمة

| ID | الخدمة | الصورة |
|----|--------|--------|
| 1 | لوحات تحكم MDB | `تصنيع-وتوريد-لوحات-MDB.webp` |
| 2 | مضخات صناعية KSB | `صيانة-مضخات-KSB-_1_.webp` |
| 3 | أعمال كهربائية | `أفضل-مؤسسة-تنفيذ-أعمال-كهرباء-في-المملكة.webp` |
| 4 | لوحات تحكم PLC | `تجميع-لوحات-التحكم.webp` |
| 5 | كهرباء وصيانة مصانع | `صيانة-كهرباء-المصانع-بالرياض.png` |
| 6 | لف المحركات الكهربائية | `لف-المحركات-الكهربائية.webp` |
| 7 | مضخات حريق UL/FM | `صيانة-مضخات-الحريق.webp` |
| 8 | مضخات ديزل | `صيانة-مضخات-الديزل.webp` |
| 9 | مضخات جراندفوس | `صيانة-مضخات-جراندفوس.webp` |
| 10 | مضخات خرسانة | `صيانة-مضخات-الخرسانة.webp` |
| 11 | محطة ضخ مياه | `صيانة-مضخات-KSB-.webp` |

---

## ✨ الميزات التقنية

### التحميل التدريجي للصور (Progressive Loading)
- تظهر placeholder ملونة فوراً
- الصور تُحمّل في الخلفية مع `loading="lazy"`
- تأثير fade-in عند اكتمال التحميل

### الفلترة الديناميكية
- فلترة المشاريع حسب الفئة (كهربائي/ميكانيكي/لف محركات)
- دعم URL hash للروابط المباشرة (`#electrical`, `#mechanical`, `#winding`)
- النقر على أي مشروع يُفلتر حسب فئته

### التوافق
- يعمل مباشرة بـ `file:///` بدون سيرفر
- يعمل مع أي استضافة ويب
- متوافق مع جميع المتصفحات الحديثة

---

## 🔧 كيفية التعديل

### إضافة مشروع جديد:
1. أضف الصورة إلى المجلد المناسب (`electrical/`, `mechanical/`, `winding/`)
2. عدّل `media/projects.json` وأضف البيانات
3. (اختياري) حدّث البيانات المضمنة في `projects.html`

### إضافة خدمة جديدة:
1. أضف الصورة إلى `media/services/`
2. عدّل `media/services/services.json`
3. (اختياري) حدّث البيانات المضمنة في `services.html`

---

## 📈 الإحصائيات

| القسم | العدد |
|-------|-------|
| الأعمال الكهربائية | 26 |
| الأعمال الميكانيكية | 20 |
| لف المحركات | 2 |
| الخدمات | 11 |
| **إجمالي المشاريع** | **48** |

---

## 📞 معلومات التواصل

- **الموقع**: [brilliantserv.com](https://brilliantserv.com)
- **الهاتف**: 0550698111 | 0543650817
- **البريد**: info@brilliantserv.com
- **العنوان**: المملكة العربية السعودية - الرياض - شارع العليا

---

*آخر تحديث: 30 يناير 2026*

---

## 🚀 UltraThink SE & Analytics Report (2026-01-30)

تقرير شامل عن حالة الـ SEO، الأدوات البرمجية، ونتائج تحليل Google Search Console.

### 1. الأدوات البرمجية (Custom Tools)
تم تطوير أدوات خاصة للمشروع لتسهيل التحليل والاستخراج:

*   **`gsc_extractor.py`**:
    *   **الوظيفة**: سكريبت Python لاستخراج بيانات الأداء من Google Search Console.
    *   **الموقع**: الجذر (Root Directory).
    *   **المتطلبات**: مكتبات `google-api-python-client`, `pandas`.
    *   **البيانات الناتجة**: ملف CSV يحتوي على (الاستعلامات، النقرات، مرات الظهور، المراكز).

*   **`seo_auditor.py`**:
    *   **الوظيفة**: أداة تدقيق محلي (Local Audit) للتحقق من العناوين, Meta descriptions, و Alt tags.
    *   **الموقع**: `.gemini/antigravity/tools/`
    *   **الاستخدام**: فحص ملفات HTML محلياً قبل الرفع.

### 2. الملفات الحساسة (Credentials)
*   **المسار**: `credentials/credentials.json`
*   **الوظيفة**: مفتاح Service Account يسمح للسكريبت بالاتصال بجوجل وسحب البيانات.
*   **الحماية**: هذا المجلد مضاف لملف `.gitignore` لضمان عدم رفعه إلى GitHub.

### 3. نتائج تحليل البيانات (Key Insights)

#### أ) الكلمات المفتاحية (Top Performance)
| الكلمة المفتاحية | مرات الظهور (Impressions) | النقر (Clicks) | النقاط المهمة |
|------------------|---------------------------|----------------|---------------|
| **انواع المضخات** | 669 | 64 | **الأعلى ظهوراً!** لكن معدل النقر يحتاج تحسين (الوصف). |
| **شركة صيانة مضخات**| 39 | 8 | **الأعلى ربحية!** نسبة نقل 20% (نية شراء عالية). |
| **لف المحركات** | 60 | 6 | أداء ثابت في المركز 2.5، تحتاج محتوى أكثر تخصصاً. |

#### ب) المشاكل المكتشفة (Critical Issues)
1.  **مشكلة الروابط (URL Slugs)**: جوجل يؤرشف العناوين العربية برموز مشفرة (مثل `%D8%A7...`) مما يقلل من جاذبية الرابط عند المشاركة ويصعب قراءته.
2.  **تشتت الـ Sitemap**: ملف `sitemap.xml` الحالي يوجه لصفحات `.html` بينما الفهرسة الحقيقية تتم على صفحات `.php` أو روابط نظيفة، مما يشتت عناكب البحث.

### 4. خارطة الطريق (Next Steps)
1.  **Immediate Fix**: تحديث `sitemap.xml` ليعكس الروابط الحقيقية التي تجلب الزوار.
2.  **Conversion**: تحسين وصف صفحة "انواع المضخات" لرفع نسبة النقر.
3.  **Content**: التركيز على محتوى "الصيانة" لأنه يجلب عملاء حقيقيين (High Intent).

---

## 🛑 تقرير العمليات: تحسين المدونة والنشر الآلي (30 يناير 2026)

في جلسة العمل (ID: `1324ef29-8b5a...`)، تم تحويل الموقع من موقع ثابت (Static) إلى منصة قابلة للتوسع لاستيعاب خطة الـ 100 مقال.

### 1. فلسفة القالب 3 (Template 3 Logic)
**"القالب ليس مجرد HTML، بل محرك محتوى."**
تم إعادة تصميم `blog.php` ليكون **ديناميكياً**:
*   **الوضع القديم**: كل مقال كان يُضاف يدوياً في كود HTML لصفحة المدونة، مما يجعل إدارته كابوساً عند الوصول لـ 50+ مقال.
*   **الوضع الجديد (Template 3)**:
    *   `blog.php` يعمل كـ **Controller** يقرأ البيانات من مصفوفة PHP (`$articles`).
    *   إضافة مقال جديد = سطر واحد فقط في المصفوفة.
    *   تحويل جميع المقالات إلى `.php` لاستخدام `header.php` و `footer.php` الموحد (تعديل واحد ينعكس عالمياً).

### 2. خطة الـ 100 مقال (Content Strategy)
*   تم إنشاء **`CONTENT_PLAN.md`** في الجذر ليكون "لوحة القيادة" (Dashboard) لخطة النشر.
*   المقالات تستهدف **GEO (Generative Engine Optimization)** عبر الإجابة المباشرة والجداول.
*   **السياسة**: كل مقال يجب أن يكون `.php` ويرتبط بملف `sitemap.xml` محدث.

### 3. استكشاف الأخطاء وإصلاحها (Troubleshooting Log)

#### أ) مشكلة Git Bash و Deploy.bat
*   **المشكلة**: عند تشغيل `ssh_connection/deploy.bat` من طرفية Git Bash، ظهر خطأ `command not found` أو `path not found`.
*   **السبب**: ملفات `.bat` مخصصة لبيئة Windows CMD ولا تفهمها Git Bash بشكل مباشر بنفس مسارات Windows (Backslashes `\`).
*   **الحل**:
    *   **للمرات القادمة**: شغل الملف من `cmd` أو `PowerShell` مباشرة: `ssh_connection\deploy.bat`.
    *   **الحل الآني**: قمنا بتنفيذ أوامر `git push` يدوياً ثم أمر `ssh` للاتصال بالسيرفر.

#### ب) تكرار المحتوى أثناء النشر
*   **المشكلة**: وجود ملفات `.html` قديمة (مثل `blog/leak-detection-jeddah.html`) بجانب النسخ الجديدة `.php`.
*   **الحل الجذري**: تحديث `templates/template-3/deploy.sh` (على السيرفر) ليقوم بمسح ملفات `.html` القديمة تلقائياً من مجلد `blog/` قبل نسخ الملفات الجديدة، لضمان نظافة السيرفر.

### 4. وصية للعمل المستقبلي
> [!IMPORTANT]
> **تحديث هذا الملف**: بعد كل "عملية جراحية" (تغيير جذري في الكود أو البنية)، يجب تحديث قسم التقارير في هذا الملف (`README.md`) ليكون مرجعاً للذكاء الاصطناعي في الجلسة التالية. هذا يوفر التوكنز ويمنح السياق فوراً.
